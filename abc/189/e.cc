#include <algorithm>
#include <cmath>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <string>
#include <vector>

using namespace std;

using ll = long long;
using ld = long double;

const ld pi = 3.141592653589793;

long n, m, q;
string op[200005];
pair<long, long> p[200005];
pair<long, long> query[200005];

int main() {
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> p[i].first >> p[i].second;
  }
  cin >> m;
  for (int i = 0; i < m; i++) {
    cin >> op[i];
  }
  cin >> q;
  for (int i = 0; i < q; i++) {
    cin >> query[i].first >> query[i].second;
  }

  for (int i = 0; i < q; i++) {
    for (int i = )
  }
}